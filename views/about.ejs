<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>About Me - <%= user.name %>'s Portfolio</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              "light-green": "#E6F7E9",
              "medium-green": "#A5D6A7",
              "dark-green": "#75C278",
            },
            animation: {
              "fade-in": "fadeIn 1s ease-in-out",
              "slide-up": "slideUp 0.7s ease-out",
              "slide-in-right": "slideInRight 0.7s ease-out",
              "slide-in-left": "slideInLeft 0.7s ease-out",
              "bounce-slow": "bounce 3s infinite",
              "pulse-slow": "pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite",
              float: "float 5s ease-in-out infinite",
              typing: "typing 3.5s steps(40, end)",
              progress: "progress 2s ease-out",
            },
            keyframes: {
              fadeIn: {
                "0%": { opacity: "0" },
                "100%": { opacity: "1" },
              },
              slideUp: {
                "0%": { transform: "translateY(50px)", opacity: "0" },
                "100%": { transform: "translateY(0)", opacity: "1" },
              },
              slideInRight: {
                "0%": { transform: "translateX(50px)", opacity: "0" },
                "100%": { transform: "translateX(0)", opacity: "1" },
              },
              slideInLeft: {
                "0%": { transform: "translateX(-50px)", opacity: "0" },
                "100%": { transform: "translateX(0)", opacity: "1" },
              },
              float: {
                "0%, 100%": { transform: "translateY(0)" },
                "50%": { transform: "translateY(-10px)" },
              },
              typing: {
                "0%": { width: "0" },
                "100%": { width: "100%" },
              },
              progress: {
                "0%": { width: "0%" },
                "100%": { width: "var(--progress-width)" },
              },
            },
          },
        },
      };
    </script>
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap");

      body {
        font-family: "Poppins", sans-serif;
        scroll-behavior: smooth;
      }

      .bg-gradient {
        background: linear-gradient(120deg, #e6f7e9, #a5d6a7, #e6f7e9);
        background-size: 200% 200%;
        animation: gradientBG 15s ease infinite;
      }

      @keyframes gradientBG {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }

      .nav-link {
        position: relative;
        transition: all 0.3s ease;
      }

      .nav-link::after {
        content: "";
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 0;
        height: 2px;
        background-color: #75c278;
        transition: width 0.3s ease;
      }

      .nav-link:hover::after,
      .nav-link.active::after {
        width: 100%;
      }

      .nav-link.active {
        color: #75c278;
        font-weight: 500;
      }

      .skill-progress {
        position: relative;
        height: 8px;
        background-color: #e6f7e9;
        border-radius: 4px;
        overflow: hidden;
      }

      .skill-bar {
        height: 100%;
        border-radius: 4px;
        background: linear-gradient(to right, #75c278, #a5d6a7);
        width: 0;
        animation: progress 1.5s ease-out forwards;
      }

      .timeline-item {
        position: relative;
        padding-left: 30px;
      }

      .timeline-item::before {
        content: "";
        position: absolute;
        left: 0;
        top: 5px;
        width: 12px;
        height: 12px;
        border-radius: 50%;
        background-color: #75c278;
        z-index: 1;
      }

      .timeline-item::after {
        content: "";
        position: absolute;
        left: 5px;
        top: 22px;
        width: 2px;
        height: calc(100% - 15px);
        background-color: #a5d6a7;
      }

      .timeline-item:last-child::after {
        display: none;
      }

      .education-card {
        transition: all 0.3s ease;
        border-left: 4px solid transparent;
      }

      .education-card:hover {
        transform: translateX(5px);
        border-left: 4px solid #75c278;
      }
    </style>
  </head>
  <body class="bg-gradient min-h-screen">
    <!-- Navigation -->
    <nav
      class="bg-white bg-opacity-90 shadow-md py-4 sticky top-0 z-10 animate-slide-down"
    >
      <div
        class="container mx-auto px-6 flex flex-wrap items-center justify-between"
      >
        <a href="/" class="text-2xl font-bold text-dark-green"
          ><%= user.name %></a
        >
        <div class="flex space-x-6">
          <a href="/" class="nav-link text-gray-700 hover:text-dark-green"
            >Home</a
          >
          <a
            href="/loadAbout"
            class="nav-link text-gray-700 hover:text-dark-green active"
            >About</a
          >
          <a
            href="/loadProject"
            class="nav-link text-gray-700 hover:text-dark-green"
            >Projects</a
          >
          <a
            href="/loadContact"
            class="nav-link text-gray-700 hover:text-dark-green"
            >Contact</a
          >
        </div>
      </div>
    </nav>

    <main class="container mx-auto px-6 py-12">
      <!-- About Heading -->
      <div class="text-center mb-16 animate-fade-in">
        <h1 class="text-4xl font-bold text-dark-green mb-4">About Me</h1>
        <div class="w-24 h-1 bg-medium-green mx-auto"></div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
        <!-- Skills Section -->
        <div class="lg:col-span-1">
          <div class="bg-white rounded-xl shadow-lg p-8 animate-slide-in-left">
            <h2 class="text-2xl font-bold text-dark-green mb-6">My Skills</h2>
            <div class="space-y-6">
              <% if (user.skills && user.skills.length > 0) { %> <%
              user.skills.forEach(skill => { %>
              <div>
                <div class="flex justify-between mb-2">
                  <span class="font-medium text-gray-700"
                    ><%= skill.name %></span
                  >
                  <span class="text-medium-green"
                    ><%= skill.proficiency * 10 %>%</span
                  >
                </div>
                <div class="skill-progress">
                  <div
                    class="skill-bar"
                    style="--progress-width: <%= skill.proficiency * 10 %>%"
                  ></div>
                </div>
              </div>
              <% }); %> <% } else { %>
              <p class="text-gray-600">No skills available.</p>
              <% } %>
            </div>
          </div>
        </div>

        <!-- Experience Section -->
        <div class="lg:col-span-2">
          <div class="bg-white rounded-xl shadow-lg p-8 animate-slide-in-right">
            <h2 class="text-2xl font-bold text-dark-green mb-6">
              Work Experience
            </h2>
            <div class="space-y-8">
              <% if (experiences && experiences.length > 0) { %> <%
              experiences.forEach(exp => { %>
              <div class="timeline-item">
                <h3 class="text-xl font-semibold text-gray-800">
                  <%= exp.position %>
                </h3>
                <div class="text-medium-green font-medium mb-2">
                  <%= exp.company %> | <%=
                  exp.startDate.toLocaleDateString('en-US', { year: 'numeric',
                  month: 'short' }) %> - <% if (exp.current) { %> Present <% }
                  else { %> <%= exp.endDate ?
                  exp.endDate.toLocaleDateString('en-US', { year: 'numeric',
                  month: 'short' }) : 'Present' %> <% } %>
                </div>
                <p class="text-gray-600"><%= exp.description %></p>
                <% if (exp.skills && exp.skills.length > 0) { %>
                <div class="mt-2 flex flex-wrap gap-2">
                  <% exp.skills.forEach(skill => { %>
                  <span
                    class="bg-light-green text-dark-green text-sm px-3 py-1 rounded-full"
                    ><%= skill %></span
                  >
                  <% }); %>
                </div>
                <% } %>
              </div>
              <% }); %> <% } else { %>
              <p class="text-gray-600">No work experience added yet.</p>
              <% } %>
            </div>
          </div>

          <!-- Education Section -->
          <div class="bg-white rounded-xl shadow-lg p-8 mt-8 animate-slide-up">
            <h2 class="text-2xl font-bold text-dark-green mb-6">Education</h2>
            <div class="space-y-6">
              <div
                class="education-card p-4 rounded bg-gray-50 hover:bg-light-green transition-all"
              >
                <h3 class="text-xl font-semibold text-gray-800">
                  Master's in Computer Science
                </h3>
                <div class="text-medium-green font-medium">
                  University Name | 2014 - 2016
                </div>
                <p class="text-gray-600 mt-1">
                  Specialized in Human-Computer Interaction and Web Technologies
                </p>
              </div>
              <div
                class="education-card p-4 rounded bg-gray-50 hover:bg-light-green transition-all"
              >
                <h3 class="text-xl font-semibold text-gray-800">
                  Bachelor's in Software Engineering
                </h3>
                <div class="text-medium-green font-medium">
                  University Name | 2010 - 2014
                </div>
                <p class="text-gray-600 mt-1">
                  Graduated with honors, Minor in Digital Design
                </p>
              </div>
              <div
                class="education-card p-4 rounded bg-gray-50 hover:bg-light-green transition-all"
              >
                <h3 class="text-xl font-semibold text-gray-800">
                  UX Design Certification
                </h3>
                <div class="text-medium-green font-medium">
                  Design Institute | 2017
                </div>
                <p class="text-gray-600 mt-1">
                  Advanced course in user experience design and research methods
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>

    <!-- Footer -->
    <footer class="bg-dark-green text-white py-8 mt-16">
      <div class="container mx-auto px-6">
        <div class="flex flex-col md:flex-row justify-between items-center">
          <div class="mb-4 md:mb-0">
            <h3 class="text-xl font-bold"><%= user.name %></h3>
            <p><%= user.professionalTitle %></p>
          </div>
          <div class="flex space-x-6">
            <% if (user.socialLinks && user.socialLinks.linkedin) { %>
            <a
              href="<%= user.socialLinks.linkedin %>"
              class="text-white hover:text-light-green transition-colors"
            >
              <i class="fab fa-linkedin text-xl"></i>
            </a>
            <% } %> <% if (user.socialLinks && user.socialLinks.github) { %>
            <a
              href="<%= user.socialLinks.github %>"
              class="text-white hover:text-light-green transition-colors"
            >
              <i class="fab fa-github text-xl"></i>
            </a>
            <% } %> <% if (user.socialLinks && user.socialLinks.twitter) { %>
            <a
              href="<%= user.socialLinks.twitter %>"
              class="text-white hover:text-light-green transition-colors"
            >
              <i class="fab fa-twitter text-xl"></i>
            </a>
            <% } %> <% if (user.socialLinks && user.socialLinks.instagram) { %>
            <a
              href="<%= user.socialLinks.instagram %>"
              class="text-white hover:text-light-green transition-colors"
            >
              <i class="fab fa-instagram text-xl"></i>
            </a>
            <% } %>
          </div>
        </div>
        <div class="mt-6 text-center">
          <p>© 2025 <%= user.name %>. All rights reserved.</p>
        </div>
      </div>
    </footer>

    <script>
      // Function to handle scroll animations
      document.addEventListener("DOMContentLoaded", function () {
        // Optional JavaScript for additional animations
      });
    </script>
  </body>
</html>
